<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="co.touchlab.testapp">

    <application
        android:name=".SampleApplication"
        android:allowBackup="true"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name="co.touchlab.touchkit.rk.ui.SplashActivity"
            android:theme="@style/AppTheme.NoActionBar"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="co.touchlab.touchkit.rk.ui.MainActivity"
            android:theme="@style/AppTheme.NoActionBar"
            android:label="@string/activities">
        </activity>

        <activity
            android:name="co.touchlab.touchkit.rk.ui.ViewTaskActivity"
            android:label="@string/app_name"
            />

        <activity
            android:name="co.touchlab.touchkit.rk.ui.UserPinActivity"
            android:label="@string/app_name"
            />

        <activity
            android:name="co.touchlab.touchkit.rk.ui.EmailVerificationActivity"
            android:label="@string/app_name"
            />

        <activity
            android:name="co.touchlab.touchkit.rk.ui.OnboardingActivity"
            android:label="@string/app_name"
            />

        <activity
            android:name="co.touchlab.touchkit.rk.ui.ConsentActivity"
            android:label="@string/signature_page_title"
            />

        <activity
            android:name="co.touchlab.touchkit.rk.ui.ViewVideoActivity"
            android:label="@string/app_name"
            />

        <activity
            android:name="co.touchlab.touchkit.rk.ui.ViewWebDocumentActivity"
            android:label="@string/app_name"
            />

    </application>

</manifest>
